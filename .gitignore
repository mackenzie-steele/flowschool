.DS_Store
.vercel
.env*
node_modules/

# marketing landing page — local exploration only, not for deploy (2026-07)
welcome.html

# audio source files — extract to .m4a; never commit/deploy the raw video
*.mov

# pose detail concept — exploration only, not for deploy (2026-08)
pose-chair.html

# generated / working files for the song scripts
scripts/.apple-cache.json
scripts/new-songs-draft.txt
scripts/new-songs-draft.txt.media.json
*.bak

# signing keys — never committed, never deployed. The MusicKit .p8 lives
# outside this repo (FLOW-SCHOOL/musickit/) and should stay there; this is a
# guard in case one is ever dropped in here by mistake. Anything holding it
# can mint Apple Music tokens as Flow School.
*.p8
*.pem
musickit/
